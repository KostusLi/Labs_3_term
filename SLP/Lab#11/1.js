class Task{
    constructor(id, name, ending)
    {
        this.id = id;
        this.name = name;
        if(ending instanceof bool)
        {
            this.ending = ending;
        }
    }

    change
}